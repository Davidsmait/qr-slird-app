import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'qr-slird-ui-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
